[build]
  command = "npm run build"
  publish = "dist"

# If you have a separate backend, add a proxy rule here (MUST be before the /* rule):
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-api.com/api/:splat"
#   status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
